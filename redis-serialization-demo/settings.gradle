pluginManagement {
    repositories {
        maven { url 'https://repo.spring.io/milestone' }
        maven { url 'https://repo1.maven.org/maven2/' }
        gradlePluginPortal()
    }
}

rootProject.name = 'redis-serialization-demo'
